# Generated manifests (regenerated by gen_book_manifest.js, not source of truth)
books/*/_manifest.json

# Temp / verification artifacts
.deploy_verify/
*.tmp

# Dependencies (managed runtimes keep these outside the repo)
node_modules/
