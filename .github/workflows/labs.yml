name: Validate 3D labs
on:
  pull_request:
    paths: ['labs/**', 'qa_labs.cjs', 'qa_models.cjs', 'index.html', 'books/**/index.html', 'shared/library-sections.css', '.github/workflows/labs.yml']
  push:
    branches: [main]
    paths: ['labs/**', 'qa_labs.cjs', 'qa_models.cjs', 'index.html', 'books/**/index.html', 'shared/library-sections.css', '.github/workflows/labs.yml']
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node qa_labs.cjs
      - run: node qa_models.cjs
